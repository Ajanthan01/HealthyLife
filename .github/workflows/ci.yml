name: integration

on:
  pull_request:
    branches: [ "main" ]

jobs:
  test-on-node:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version: [18.x, 20.x, 22.x]

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Build the app
        run: npm run build

      - name: Run unit tests
        run: npm run test:unit  # Run unit tests using the "test:unit" script

      - name: Run integration tests
        run: npm run test:integration  # Run integration tests using the "test:integration" script

      - name: Run E2E tests
        run: npm run test:e2e  # Run E2E tests using the "test:e2e" script
